# v0.4 ModelBox Flowunit Skill Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a curated ModelBox flowunit skill — 19 reference files + a routing SKILL.md + a reproducible generator script + a CI auto-bump workflow — so Claude can author ModelBox graphs without reading the upstream C++ codebase.

**Architecture:** A Python generator (`build-modelbox-skill.py`) reads a hand-curated allowlist YAML and optionally pulls TOML snippets from the pinned upstream modelbox repo, stitching together `skills/modelbox/SKILL.md` (catalog + virtual-type matrix + recipes) and 19 `references/<name>.md` files. Generated files are checked in as the runtime source of truth. A GitHub Actions workflow checks upstream for drift daily and opens a PR when the pin falls behind. A validator script (`test-modelbox-skill.py`) enforces the six-section reference contract and catalog/file parity.

**Tech Stack:** Python 3.11+ (tomllib, pyyaml), GitHub Actions, ModelBox graphviz-TOML graph DSL.

---

## File Structure

```
tools/
  modelbox-flowunits.yml          # Task 1 — allowlist: 19 flowunits, pinned SHA, all metadata
  modelbox-skill-prelude.md       # Task 2 — hand-written SKILL.md sections (non-catalog)
  modelbox-skill-recipes.md       # Task 3 — four pipeline recipe skeletons
  test-modelbox-skill.py          # Task 4 — validator (TDD: write before generator)
  build-modelbox-skill.py         # Task 5 — generator script

skills/modelbox/
  SKILL.md                        # Task 6 — generated (checked in)
  references/
    inference.md                  # Task 6 — generated (checked in)
    coreml_inference.md
    video_input.md
    video_demuxer.md
    video_decoder.md
    video_encoder.md
    resize.md
    image_process.md
    normalize.md
    mean.md
    yolo26_post.md
    yolo_seg_post.md
    yolo_pose_post.md
    yolo_track_post.md
    draw_bbox.md
    data_source_generator.md
    httpserver_async.md
    output_broker.md
    meta_mapping.md

.github/workflows/
  modelbox-bump.yml               # Task 7 — CI auto-bump

.claude-plugin/
  plugin.json                     # Task 8 — bump to 0.4.0
  marketplace.json                # Task 8 — bump to 0.4.0
```

---

### Task 1: `tools/modelbox-flowunits.yml` — Allowlist YAML

**Files:**
- Create: `tools/modelbox-flowunits.yml`

This file is the single source of truth: upstream pin, per-flowunit metadata (purpose, ports, config keys, notes, fallback TOML). The generator reads it; so does the validator.

- [ ] **Step 1: Create `tools/modelbox-flowunits.yml`**

```yaml
modelbox_repo: https://github.com/modelbox-ai/modelbox.git
modelbox_branch: master
modelbox_commit: 125d1cd6b746cbfd410ac288a1e1f2e2664fb77e
modelbox_commit_date: "2026-05-06"

flowunits:

  # ── Inference ──────────────────────────────────────────────────────────────

  - name: inference
    group: Inference
    devices: [cuda, cpu]
    use_for: Run any model; virtual_type picks engine
    purpose: |
      Generic inference flowunit. Loads a model file and runs it through the
      engine selected by `virtual_type`. Supports TensorRT, Torch, ONNX Runtime,
      ACL, MindSpore, and OpenVINO via a single TOML interface — swap
      `virtual_type` and `entry` to change chip target.
    source_dir: src/drivers/common/flowunit/inference
    example_toml: examples/flowunit/infer/example.toml
    default_toml: ""
    ports:
      inputs:
        - name: input
          type: float tensor
          notes: NHWC float32 [1,H,W,3] by default; shape depends on model
      outputs:
        - name: output
          type: float tensor
          notes: Model-defined shape
    config_keys:
      - key: entry
        required: true
        default: "—"
        notes: Path to model file, relative to the flowunit directory
      - key: virtual_type
        required: true
        default: "—"
        notes: "tensorrt | torch | onnxruntime | acl | mindspore | openvino"
      - key: device
        required: true
        default: "—"
        notes: "cuda | cpu | ascend | intel_gpu"
      - key: plugin
        required: false
        default: '""'
        notes: '"yolo" enables upsample layer for legacy YOLOv3 only'
    notes:
      - "`entry` is relative to the flowunit's own directory, not the graph TOML"
      - Use `plugin=\"yolo\"` only for YOLOv3 upsample compat; omit for all other models
      - Input tensor shape must match the model's expected input exactly

  - name: coreml_inference
    group: Inference
    devices: [apple_silicon]
    use_for: Run CoreML models on Apple Silicon (M-series)
    purpose: |
      Apple Silicon inference flowunit. Loads a CoreML model (.mlmodelc or
      .mlpackage) and runs it via the CoreML framework on the Neural Engine /
      GPU. Use in place of `inference` when targeting Apple Silicon — set
      `device=apple_silicon` and `virtual_type=coreml`.
    source_dir: src/drivers/devices/apple_silicon/flowunit/coreml_inference
    example_toml: src/demo/apple_silicon_yolo/flowunit/coreml_inference/coreml_inference.toml
    default_toml: ""
    ports:
      inputs:
        - name: input
          type: image tensor
          notes: Float32 tensor in model's expected layout
      outputs:
        - name: output
          type: float tensor
          notes: Model-defined shape
    config_keys:
      - key: entry
        required: true
        default: "—"
        notes: Path to .mlmodelc or .mlpackage, relative to flowunit directory
      - key: device
        required: true
        default: apple_silicon
        notes: Must be `apple_silicon`; any other value selects a different engine
      - key: virtual_type
        required: true
        default: coreml
        notes: Must be `coreml`
    notes:
      - device must be `apple_silicon` exactly; omitting or using `cpu` will fail
      - Pair with `video_encoder` using `encoder=h264_videotoolbox` on macOS for hardware encode
      - Export model with `yolo export format=coreml` before use

  # ── Sources ────────────────────────────────────────────────────────────────

  - name: video_input
    group: Input
    devices: [cpu]
    use_for: Open a video file and stream decoded frames
    purpose: |
      File-based video source. Opens a video file and streams decoded frames.
      Preferred over `data_source_generator` when the source is a single video
      file — it handles container parsing and codec negotiation internally.
      Emits decoded image frames directly; no separate demuxer/decoder needed.
    source_dir: src/drivers/common/flowunit/video_input
    example_toml: ""
    default_toml: |
      [base]
      name = "video_input"
      device = "cpu"
      version = "1.0.0"
      type = "video_input"

      [config]
      source_url = "${input_video}"
    ports:
      inputs: []
      outputs:
        - name: out_video_url
          type: string
          notes: Source URL forwarded to downstream demuxer/decoder chain
    config_keys:
      - key: source_url
        required: true
        default: "—"
        notes: File path or URL to the video file; supports ${var} expansion
    notes:
      - Prefer this over data_source_generator for single video file inputs
      - Does not demux/decode itself — wire out_video_url into video_demuxer

  - name: data_source_generator
    group: Input
    devices: [cpu]
    use_for: File/dir source (replays paths into a stream)
    purpose: |
      Generic file/directory source. Replays file paths (or directory globs)
      into a stream, one path per data item. Use for image directories or when
      you need fine-grained control over batching and retries. Prefer
      `video_input` for single video files.
    source_dir: src/drivers/common/flowunit/data_source_generator
    example_toml: ""
    default_toml: |
      [base]
      name = "data_source_generator"
      device = "cpu"
      version = "1.0.0"
      type = "data_source_generator"

      [config]
      source_url = "${input_path}"
      retry_count = 3
      retry_interval = 1
    ports:
      inputs: []
      outputs:
        - name: out_data
          type: string
          notes: File path emitted for each item in the source
    config_keys:
      - key: source_url
        required: true
        default: "—"
        notes: File path, directory glob, or URL
      - key: retry_count
        required: false
        default: "3"
        notes: Number of retries on source open failure
      - key: retry_interval
        required: false
        default: "1"
        notes: Seconds between retries
    notes:
      - For video files use video_input instead (simpler wiring)
      - Directory globs emit one path per matching file in lexicographic order

  - name: httpserver_async
    group: Input
    devices: [cpu]
    use_for: HTTP request/reply edges
    purpose: |
      Async HTTP server edge. Exposes an HTTP endpoint; each inbound request
      becomes one pipeline data item. Pairs with a reply port on a downstream
      flowunit to send the HTTP response. Use for request/reply inference APIs.
    source_dir: src/drivers/common/flowunit/httpserver_async
    example_toml: ""
    default_toml: |
      [base]
      name = "httpserver_async"
      device = "cpu"
      version = "1.0.0"
      type = "httpserver_async"

      [config]
      endpoint = "/api/detect"
      port = 8080
      max_requests = 10
    ports:
      inputs:
        - name: in_reply
          type: json/bytes
          notes: HTTP response body; wire from final result flowunit
      outputs:
        - name: out_request
          type: bytes
          notes: Raw HTTP request body for each inbound call
    config_keys:
      - key: endpoint
        required: true
        default: "—"
        notes: URL path to listen on, e.g. /api/detect
      - key: port
        required: true
        default: "8080"
        notes: TCP port
      - key: max_requests
        required: false
        default: "10"
        notes: Max in-flight concurrent requests
    notes:
      - The reply port must be wired back from the last flowunit in the pipeline
      - One request = one pipeline execution; async means requests don't block each other

  # ── Video pipeline ──────────────────────────────────────────────────────────

  - name: video_demuxer
    group: Video
    devices: [cpu]
    use_for: Split mp4/mkv container into elementary streams
    purpose: |
      Splits a video container (mp4, mkv, ts, …) into elementary streams.
      Emits raw encoded packets; wire into video_decoder. Use when you need
      explicit demux/decode separation (e.g. to route audio and video
      independently). For simple file-to-frame pipelines prefer video_input.
    source_dir: src/drivers/common/flowunit/video_demuxer
    example_toml: ""
    default_toml: |
      [base]
      name = "video_demuxer"
      device = "cpu"
      version = "1.0.0"
      type = "video_demuxer"
    ports:
      inputs:
        - name: in_video_url
          type: string
          notes: Video file path or URL from video_input
      outputs:
        - name: out_video_packet
          type: video_packet
          notes: Encoded packet stream for video_decoder
    config_keys: []
    notes:
      - Wire out_video_packet directly into video_decoder:in_video_packet
      - Audio packets are currently dropped; video only

  - name: video_decoder
    group: Video
    devices: [cuda, cpu]
    use_for: Decode H.264/H.265 to frames (NV12/RGB)
    purpose: |
      Decodes H.264/H.265 packets from video_demuxer into raw frames. The
      `cuda` device uses NVDEC hardware decoder; `cpu` falls back to FFmpeg
      software decode. Output pixel format is configurable: NV12 (default on
      cuda, best for GPU preprocessing), or RGB/BGR (for cpu preprocessing).
    source_dir: src/drivers/common/flowunit/video_decoder
    example_toml: ""
    default_toml: |
      [base]
      name = "video_decoder"
      device = "cuda"
      version = "1.0.0"
      type = "video_decoder"

      [config]
      pix_fmt = "rgb"
    ports:
      inputs:
        - name: in_video_packet
          type: video_packet
          notes: Encoded packet from video_demuxer
      outputs:
        - name: out_image
          type: image
          notes: Decoded frame in configured pix_fmt
    config_keys:
      - key: pix_fmt
        required: false
        default: nv12
        notes: "nv12 | rgb | bgr — NV12 preferred between cuda decode and cuda preprocess"
    notes:
      - cuda device uses NVDEC; pix_fmt=nv12 avoids a CPU copy when feeding image_process on cuda
      - device mismatch with next flowunit is acceptable but adds a memory copy

  - name: video_encoder
    group: Video
    devices: [cpu]
    use_for: Frames → H.264 mp4
    purpose: |
      Encodes frames to H.264 and muxes into an mp4 container. On macOS set
      `encoder=h264_videotoolbox` for hardware encode. On Linux with Intel
      Quick Sync use `encoder=h264_qsv`. Default `libx264` works on any OS.
    source_dir: src/drivers/common/flowunit/video_encoder
    example_toml: ""
    default_toml: |
      [base]
      name = "video_encoder"
      device = "cpu"
      version = "1.0.0"
      type = "video_encoder"

      [config]
      dest_url = "${output_video}"
      encoder = "libx264"   # macOS: h264_videotoolbox | Linux/QSV: h264_qsv
      bitrate = 2097152     # bits/s (2 Mbps)
    ports:
      inputs:
        - name: in_image
          type: image
          notes: Annotated frame (RGB or BGR)
      outputs: []
    config_keys:
      - key: dest_url
        required: true
        default: "—"
        notes: Output file path; supports ${var} expansion
      - key: encoder
        required: false
        default: libx264
        notes: "libx264 (any) | h264_videotoolbox (macOS) | h264_qsv (Linux/Intel)"
      - key: bitrate
        required: false
        default: "2097152"
        notes: Target bitrate in bits/s
    notes:
      - Use h264_videotoolbox on macOS for hardware encode (Apple Silicon pipeline)
      - dest_url directory must exist before the pipeline starts

  # ── Preprocessing ───────────────────────────────────────────────────────────

  - name: resize
    group: Preprocess
    devices: [cpu]
    use_for: Resize frames to a fixed width × height
    purpose: |
      Standalone resize flowunit. Scales frames to a target width/height.
      Simpler than image_process when you only need resize — one config key
      each for width and height, no colorspace or layout options. Used in the
      Apple Silicon pipeline (resize → coreml_inference) where image_process
      is not available on apple_silicon device.
    source_dir: src/drivers/common/flowunit/resize
    example_toml: ""
    default_toml: |
      [base]
      name = "resize"
      device = "cpu"
      version = "1.0.0"
      type = "resize"

      [config]
      width = 640
      height = 640
    ports:
      inputs:
        - name: in_image
          type: image
          notes: Input frame; any resolution
      outputs:
        - name: out_image
          type: image
          notes: Resized frame at configured width × height
    config_keys:
      - key: width
        required: true
        default: "—"
        notes: Target width in pixels
      - key: height
        required: true
        default: "—"
        notes: Target height in pixels
    notes:
      - No letterboxing — frame is stretched; use image_process for letterbox
      - Device variants available for ascend and rockchip (not in v0.4 allowlist)

  - name: image_process
    group: Preprocess
    devices: [cuda, cpu]
    use_for: Resize/letterbox/colorspace/layout convert
    purpose: |
      Generic image-processing flowunit. Handles resize, letterbox padding,
      colorspace conversion (BGR↔RGB, NV12→RGB), and memory layout conversion
      (HWC↔CHW). The Swiss-army knife for building a preprocessing chain before
      inference on NVIDIA. Prefer `resize` for Apple Silicon (apple_silicon device
      is not supported by image_process).
    source_dir: src/drivers/common/flowunit/image_process
    example_toml: ""
    default_toml: |
      [base]
      name = "image_process"
      device = "cuda"
      version = "1.0.0"
      type = "image_process"

      [config]
      width = 640
      height = 640
      interpolation = "inter_linear"
      color_mode = "bgr"
      data_type = "float"
    ports:
      inputs:
        - name: in_image
          type: image
          notes: Input frame (NV12 from cuda decoder or RGB/BGR from cpu decoder)
      outputs:
        - name: out_image
          type: image/tensor
          notes: Preprocessed tensor in configured layout and dtype
    config_keys:
      - key: width
        required: true
        default: "—"
        notes: Target width
      - key: height
        required: true
        default: "—"
        notes: Target height
      - key: interpolation
        required: false
        default: inter_linear
        notes: "inter_linear | inter_nearest | inter_cubic"
      - key: color_mode
        required: false
        default: bgr
        notes: "bgr | rgb — output colorspace"
      - key: data_type
        required: false
        default: float
        notes: "float | uint8"
    notes:
      - NV12 input from cuda video_decoder is handled automatically when device=cuda
      - Does not support apple_silicon device; use resize for Apple Silicon pipelines

  - name: normalize
    group: Preprocess
    devices: [cuda, cpu]
    use_for: Per-channel normalize (mean/std)
    purpose: |
      Per-channel normalization. Applies `(pixel - mean) / std` to each channel.
      Configure `mean` and `std` as comma-separated floats matching the channel
      count. Use when the model expects ImageNet-normalized input.
    source_dir: src/drivers/common/flowunit/normalize
    example_toml: ""
    default_toml: |
      [base]
      name = "normalize"
      device = "cuda"
      version = "1.0.0"
      type = "normalize"

      [config]
      mean = "0.485, 0.456, 0.406"
      std = "0.229, 0.224, 0.225"
    ports:
      inputs:
        - name: in_data
          type: float tensor
          notes: Float32 tensor from image_process
      outputs:
        - name: out_data
          type: float tensor
          notes: Normalized float32 tensor
    config_keys:
      - key: mean
        required: true
        default: "—"
        notes: Comma-separated per-channel mean values (e.g. "0.485, 0.456, 0.406")
      - key: std
        required: true
        default: "—"
        notes: Comma-separated per-channel std values (e.g. "0.229, 0.224, 0.225")
    notes:
      - Channel count inferred from comma count; must match the input tensor
      - YOLO models typically do not need this (internal normalization in the model)

  - name: mean
    group: Preprocess
    devices: [cuda, cpu]
    use_for: Per-channel mean subtract
    purpose: |
      Per-channel mean subtraction. Subtracts a constant from each channel.
      Simpler than `normalize` when std=1.0 (no division needed). Used by
      some older detection models (e.g. SSD variants with mean=[104,117,123]).
    source_dir: src/drivers/common/flowunit/mean
    example_toml: ""
    default_toml: |
      [base]
      name = "mean"
      device = "cuda"
      version = "1.0.0"
      type = "mean"

      [config]
      mean = "104.0, 117.0, 123.0"
    ports:
      inputs:
        - name: in_data
          type: float tensor
          notes: Float32 tensor
      outputs:
        - name: out_data
          type: float tensor
          notes: Mean-subtracted float32 tensor
    config_keys:
      - key: mean
        required: true
        default: "—"
        notes: Comma-separated per-channel mean values
    notes:
      - Use normalize instead when you also need to divide by std

  # ── YOLO Postprocessing ─────────────────────────────────────────────────────

  - name: yolo26_post
    group: Postprocess
    devices: [cpu]
    use_for: YOLO detect head → boxes (anchors, NMS)
    purpose: |
      YOLO detect-head postprocessor. Converts raw model output tensors into
      bounding boxes + class labels + confidence scores, then applies NMS.
      Supports YOLO v2–v6 anchor-based and anchor-free variants via `anchors`
      config. Use for standard object detection tasks.
    source_dir: src/drivers/common/flowunit/yolo26_post
    example_toml: ""
    default_toml: |
      [base]
      name = "yolo26_post"
      device = "cpu"
      version = "1.0.0"
      type = "yolo26_post"

      [config]
      classes = 80
      conf_threshold = 0.25
      nms_threshold = 0.45
      input_width = 640
      input_height = 640
    ports:
      inputs:
        - name: in_data
          type: float tensor
          notes: Raw model output from inference flowunit
      outputs:
        - name: out_data
          type: json
          notes: Detection results — list of {box, class_id, confidence}
    config_keys:
      - key: classes
        required: true
        default: "80"
        notes: Number of classes (must match model head)
      - key: conf_threshold
        required: false
        default: "0.25"
        notes: Minimum confidence to keep a detection
      - key: nms_threshold
        required: false
        default: "0.45"
        notes: IoU threshold for NMS suppression
      - key: input_width
        required: true
        default: "640"
        notes: Model input width (used to scale boxes back to frame coords)
      - key: input_height
        required: true
        default: "640"
        notes: Model input height
    notes:
      - input_width/input_height must match the image_process output size
      - For anchor-free YOLO11/v8, anchors config is not needed

  - name: yolo_seg_post
    group: Postprocess
    devices: [cpu]
    use_for: YOLO segment head → masks
    purpose: |
      YOLO segmentation-head postprocessor. Extends yolo26_post with per-instance
      mask decoding. Outputs bounding boxes + instance masks. Requires the model
      to emit both the detection head and the mask prototype head outputs.
    source_dir: src/drivers/common/flowunit/yolo_seg_post
    example_toml: ""
    default_toml: |
      [base]
      name = "yolo_seg_post"
      device = "cpu"
      version = "1.0.0"
      type = "yolo_seg_post"

      [config]
      classes = 80
      conf_threshold = 0.25
      nms_threshold = 0.45
      input_width = 640
      input_height = 640
    ports:
      inputs:
        - name: in_det
          type: float tensor
          notes: Detection head output
        - name: in_proto
          type: float tensor
          notes: Mask prototype head output
      outputs:
        - name: out_data
          type: json
          notes: Detection results with mask arrays
    config_keys:
      - key: classes
        required: true
        default: "80"
        notes: Number of classes
      - key: conf_threshold
        required: false
        default: "0.25"
        notes: Minimum confidence
      - key: nms_threshold
        required: false
        default: "0.45"
        notes: IoU threshold for NMS
      - key: input_width
        required: true
        default: "640"
        notes: Model input width
      - key: input_height
        required: true
        default: "640"
        notes: Model input height
    notes:
      - Requires a segmentation model export (yolo export format=tensorrt task=segment)
      - Two input ports — both must be wired from the inference flowunit

  - name: yolo_pose_post
    group: Postprocess
    devices: [cpu]
    use_for: YOLO pose head → keypoints
    purpose: |
      YOLO pose-estimation postprocessor. Decodes keypoint coordinates and
      visibility flags from the model output. Outputs bounding boxes +
      keypoints arrays. Configure `num_keypoints` to match the model
      (17 for COCO, 26 for animal pose, etc.).
    source_dir: src/drivers/common/flowunit/yolo_pose_post
    example_toml: ""
    default_toml: |
      [base]
      name = "yolo_pose_post"
      device = "cpu"
      version = "1.0.0"
      type = "yolo_pose_post"

      [config]
      classes = 1
      conf_threshold = 0.25
      nms_threshold = 0.45
      input_width = 640
      input_height = 640
      num_keypoints = 17
    ports:
      inputs:
        - name: in_data
          type: float tensor
          notes: Raw pose model output
      outputs:
        - name: out_data
          type: json
          notes: Detection results with keypoints [[x,y,vis], ...]
    config_keys:
      - key: classes
        required: true
        default: "1"
        notes: Number of classes (usually 1 = person for pose)
      - key: num_keypoints
        required: true
        default: "17"
        notes: Keypoints per instance (17 = COCO human)
      - key: conf_threshold
        required: false
        default: "0.25"
        notes: Minimum confidence
      - key: nms_threshold
        required: false
        default: "0.45"
        notes: IoU NMS threshold
      - key: input_width
        required: true
        default: "640"
        notes: Model input width
      - key: input_height
        required: true
        default: "640"
        notes: Model input height
    notes:
      - num_keypoints must match the exported model exactly

  - name: yolo_track_post
    group: Postprocess
    devices: [cpu]
    use_for: YOLO + tracker → tracked boxes with IDs
    purpose: |
      YOLO + tracker postprocessor. Wraps detect-head decoding with a ByteTrack
      or SORT tracker to assign persistent IDs across frames. Outputs bounding
      boxes with a `track_id` field. Use when you need to follow objects over
      time, not just detect them per frame.
    source_dir: src/drivers/common/flowunit/yolo_track_post
    example_toml: ""
    default_toml: |
      [base]
      name = "yolo_track_post"
      device = "cpu"
      version = "1.0.0"
      type = "yolo_track_post"

      [config]
      classes = 80
      conf_threshold = 0.25
      nms_threshold = 0.45
      input_width = 640
      input_height = 640
      tracker = "bytetrack"
    ports:
      inputs:
        - name: in_data
          type: float tensor
          notes: Raw model output from inference
      outputs:
        - name: out_data
          type: json
          notes: Detection results with persistent track_id per instance
    config_keys:
      - key: classes
        required: true
        default: "80"
        notes: Number of classes
      - key: tracker
        required: false
        default: bytetrack
        notes: "bytetrack | sort"
      - key: conf_threshold
        required: false
        default: "0.25"
        notes: Minimum confidence
      - key: nms_threshold
        required: false
        default: "0.45"
        notes: IoU NMS threshold
      - key: input_width
        required: true
        default: "640"
        notes: Model input width
      - key: input_height
        required: true
        default: "640"
        notes: Model input height
    notes:
      - track_id is stable within a pipeline run; resets on restart
      - For frame-by-frame (no tracking), use yolo26_post instead

  # ── Output / Utility ────────────────────────────────────────────────────────

  - name: draw_bbox
    group: Output
    devices: [cpu]
    use_for: Draw boxes/labels on frames
    purpose: |
      Draws bounding boxes, class labels, and confidence scores onto frames
      in-place. Input: decoded frame + detection JSON from a post flowunit.
      Output: annotated frame ready for video_encoder. Configure color,
      thickness, and font_size.
    source_dir: src/drivers/common/flowunit/draw_bbox
    example_toml: ""
    default_toml: |
      [base]
      name = "draw_bbox"
      device = "cpu"
      version = "1.0.0"
      type = "draw_bbox"

      [config]
      thickness = 2
      font_size = 0.6
    ports:
      inputs:
        - name: in_frame
          type: image
          notes: Original decoded frame (pass through from video_decoder)
        - name: in_boxes
          type: json
          notes: Detection JSON from yolo26_post / yolo_seg_post / yolo_pose_post
      outputs:
        - name: out_image
          type: image
          notes: Annotated frame
    config_keys:
      - key: thickness
        required: false
        default: "2"
        notes: Box line thickness in pixels
      - key: font_size
        required: false
        default: "0.6"
        notes: Label font scale
    notes:
      - Two input ports — both must be wired (frame + boxes)
      - The original frame must be wired directly from video_decoder, bypassing inference/postprocess

  - name: output_broker
    group: Output
    devices: [cpu]
    use_for: Send results to Kafka/RocketMQ/HTTP/file
    purpose: |
      Pluggable result sink. Routes pipeline output to Kafka, RocketMQ, an
      HTTP endpoint, or a local file — configured by `broker_type`. Use when
      the pipeline result should be consumed by an external system rather than
      written to a video file.
    source_dir: src/drivers/common/flowunit/output_broker
    example_toml: ""
    default_toml: |
      [base]
      name = "output_broker"
      device = "cpu"
      version = "1.0.0"
      type = "output_broker"

      [config]
      broker_type = "http"
      broker_url = "http://localhost:9090/results"
    ports:
      inputs:
        - name: in_data
          type: json/bytes
          notes: Detection result or any serializable payload
      outputs: []
    config_keys:
      - key: broker_type
        required: true
        default: "—"
        notes: "kafka | rocketmq | http | file"
      - key: broker_url
        required: true
        default: "—"
        notes: "Kafka/RocketMQ broker URL, HTTP endpoint, or file path"
    notes:
      - For Kafka/RocketMQ, broker_url is the bootstrap server; add `topic` config
      - For file output, broker_url is the output file path

  - name: meta_mapping
    group: Utility
    devices: [cpu]
    use_for: Rename/project/drop fields between units
    purpose: |
      Field-projection flowunit. Renames, reorders, or drops metadata fields
      between flowunits. Useful when one flowunit emits a field under a name
      that the next expects under a different name, without writing a custom
      flowunit. Configure `mappings` as a JSON object of {new_name: old_name}.
    source_dir: src/drivers/common/flowunit/meta_mapping
    example_toml: ""
    default_toml: |
      [base]
      name = "meta_mapping"
      device = "cpu"
      version = "1.0.0"
      type = "meta_mapping"

      [config]
      mappings = "{\"out_key\": \"in_key\"}"
    ports:
      inputs:
        - name: in_data
          type: any
          notes: Data item with fields to remap
      outputs:
        - name: out_data
          type: any
          notes: Data item with remapped fields
    config_keys:
      - key: mappings
        required: true
        default: "{}"
        notes: JSON object {new_field_name -> old_field_name}
    notes:
      - Fields not mentioned in mappings are passed through unchanged
      - Use to bridge naming differences without writing a custom C++ flowunit
```

- [ ] **Step 2: Commit**

```bash
git add tools/modelbox-flowunits.yml
git commit -m "feat(modelbox): add flowunit allowlist YAML (v0.4, 19 flowunits)"
```

---

### Task 2: `tools/modelbox-skill-prelude.md` — Hand-written SKILL.md sections

**Files:**
- Create: `tools/modelbox-skill-prelude.md`

The generator reads this file and extracts named sections by `<!-- SECTION: id -->` markers to stitch into SKILL.md.

- [ ] **Step 1: Create `tools/modelbox-skill-prelude.md`**

```markdown
<!-- SECTION: when_to_use -->
## When to use

Invoke this skill when you need to:

- Author or modify a ModelBox graph (`.toml` file in graphviz DSL)
- Configure a flowunit's TOML block (`[base]`, `[config]`, `[input.*]`, `[output.*]`)
- Choose the right flowunit for a pipeline step (source, decode, preprocess, infer, postprocess, encode, sink)
- Resolve "which flowunit handles X?" questions
- Pick the right `virtual_type` / device for a target chip

<!-- SECTION: mental_model -->
## Core mental model

A ModelBox pipeline is a directed graph of **flowunits** wired together in a `.toml` graph file using graphviz DSL. Each flowunit declares a `device` (cuda, cpu, apple_silicon, …), consumes typed input ports, and emits typed output ports. The graph file lists nodes (flowunit instances) and edges (port-to-port wires). Each flowunit node names a flowunit directory that holds its own `<name>.toml` config. See [modelbox-ai.com/modelbox-book](https://modelbox-ai.com/modelbox-book/) for the authoring guide.

<!-- SECTION: virtual_type_matrix -->
## Virtual-type matrix

Use `virtual_type` inside the `[base]` block of an inference flowunit to select the engine:

| Chip | `virtual_type` | `device` key | Inference flowunit |
|------|---------------|--------------|-------------------|
| NVIDIA GPU | `tensorrt` | `cuda` | `inference` |
| NVIDIA GPU | `torch` | `cuda` | `inference` |
| Apple Silicon (M-series) | `coreml` | `apple_silicon` | `coreml_inference` |
| Huawei Ascend | `acl` | `ascend` | `inference` |
| Huawei Ascend | `mindspore` | `ascend` | `inference` |
| Intel Arc / iGPU | `openvino` | `intel_gpu` | `inference` |
| CPU fallback | `onnxruntime` | `cpu` | `inference` |

<!-- SECTION: gotchas -->
## Authoring gotchas

- **Device mismatch between adjacent units** forces a memory copy (acceptable, but log it — watch for excess copies in the ModelBox console).
- **`inference`'s `entry` path** is relative to the flowunit's own directory, not the graph `.toml`.
- **Image layout:** cuda flowunits prefer NHWC input; NV12 is the native format between `video_decoder` (cuda) and `image_process`.
- **`name=` in `[base]`** must match the flowunit directory name exactly — ModelBox uses this to locate the directory.
- **Port wiring** uses `:port_name` syntax; types must match across the edge (image, tensor, json, …).
- **`coreml_inference`** requires `device=apple_silicon`; using `device=cpu` silently routes to a different engine.
- **`draw_bbox` needs two input ports** wired simultaneously — the original frame (from decoder, bypassing the infer chain) and the detection JSON (from the post flowunit).

<!-- SECTION: pointers -->
## Pointers

- [ModelBox documentation](https://modelbox-ai.com/modelbox-book/)
- [Upstream source](https://github.com/modelbox-ai/modelbox)
- See the `yolo` skill for the Ultralytics side of the pipeline: training, export to TensorRT/CoreML, dataset validation.
```

- [ ] **Step 2: Commit**

```bash
git add tools/modelbox-skill-prelude.md
git commit -m "feat(modelbox): add hand-written SKILL.md prelude sections"
```

---

### Task 3: `tools/modelbox-skill-recipes.md` — Four pipeline recipe skeletons

**Files:**
- Create: `tools/modelbox-skill-recipes.md`

Each recipe shows the full graph DSL skeleton and lists which references to read to fill in configs. Recipes 1 and 2 are byte-similar — the three lines that differ are called out explicitly.

- [ ] **Step 1: Create `tools/modelbox-skill-recipes.md`**

```markdown
<!-- SECTION: recipes -->
## Known-good pipelines

Four canned recipes. Each shows a complete graph skeleton; fill in flowunit configs from the linked references.

---

### Recipe 1: NVIDIA — file → detect → annotate → file

**Chip:** NVIDIA GPU | **Engine:** TensorRT | **References:** inference, video_input, video_demuxer, video_decoder, image_process, yolo26_post, draw_bbox, video_encoder

```toml
# graph/nvidia_file_detect.toml
[driver]
  skip-default = false
  dir = ["${MODELBOX_SOLUTION_PATH}/flowunit"]

[graph]
  format = "graphviz"
  graphconf = """digraph nvidia_file_detect {
    node [shape=Mrecord]

    video_input[type=flowunit, flowunit=video_input, device=cpu,
                source_url="${input_video}"]
    video_demuxer[type=flowunit, flowunit=video_demuxer, device=cpu]
    video_decoder[type=flowunit, flowunit=video_decoder, device=cuda,
                  pix_fmt="rgb"]
    image_process[type=flowunit, flowunit=image_process, device=cuda,
                  width=640, height=640, interpolation="inter_linear",
                  color_mode="rgb", data_type="float"]
    detect[type=flowunit, flowunit=detect, device=cuda,
           virtual_type="tensorrt"]
    yolo26_post[type=flowunit, flowunit=yolo26_post, device=cpu,
                classes=80, conf_threshold=0.25, nms_threshold=0.45,
                input_width=640, input_height=640]
    draw_bbox[type=flowunit, flowunit=draw_bbox, device=cpu,
              thickness=2, font_size=0.6]
    video_encoder[type=flowunit, flowunit=video_encoder, device=cpu,
                  dest_url="${output_video}", encoder="libx264"]

    video_input:out_video_url   -> video_demuxer:in_video_url
    video_demuxer:out_video_packet -> video_decoder:in_video_packet
    video_decoder:out_image     -> image_process:in_image
    image_process:out_image     -> detect:input
    detect:output               -> yolo26_post:in_data
    video_decoder:out_image     -> draw_bbox:in_frame
    yolo26_post:out_data        -> draw_bbox:in_boxes
    draw_bbox:out_image         -> video_encoder:in_image
  }"""
```

> The `detect` flowunit directory must contain a `.toml` with `entry=./model.engine` and `virtual_type=tensorrt`. See [references/inference.md](references/inference.md).

---

### Recipe 2: Apple Silicon — file → detect → annotate → file

**Chip:** Apple M-series | **Engine:** CoreML | **References:** coreml_inference, video_input, video_demuxer, video_decoder, resize, yolo26_post, draw_bbox, video_encoder

This is Recipe 1 with three lines changed (marked `# ← changed`):

```toml
# graph/apple_file_detect.toml
[driver]
  skip-default = false
  dir = ["${MODELBOX_SOLUTION_PATH}/flowunit"]

[graph]
  format = "graphviz"
  graphconf = """digraph apple_file_detect {
    node [shape=Mrecord]

    video_input[type=flowunit, flowunit=video_input, device=cpu,
                source_url="${input_video}"]
    video_demuxer[type=flowunit, flowunit=video_demuxer, device=cpu]
    video_decoder[type=flowunit, flowunit=video_decoder, device=cpu,
                  pix_fmt="rgb"]
    resize[type=flowunit, flowunit=resize, device=cpu,             # ← changed
           width=640, height=640]
    detect[type=flowunit, flowunit=detect, device=apple_silicon,   # ← changed
           virtual_type="coreml"]
    yolo26_post[type=flowunit, flowunit=yolo26_post, device=cpu,
                classes=80, conf_threshold=0.25, nms_threshold=0.45,
                input_width=640, input_height=640]
    draw_bbox[type=flowunit, flowunit=draw_bbox, device=cpu,
              thickness=2, font_size=0.6]
    video_encoder[type=flowunit, flowunit=video_encoder, device=cpu,
                  dest_url="${output_video}",
                  encoder="h264_videotoolbox"]                      # ← changed

    video_input:out_video_url      -> video_demuxer:in_video_url
    video_demuxer:out_video_packet -> video_decoder:in_video_packet
    video_decoder:out_image        -> resize:in_image
    resize:out_image               -> detect:input
    detect:output                  -> yolo26_post:in_data
    video_decoder:out_image        -> draw_bbox:in_frame
    yolo26_post:out_data           -> draw_bbox:in_boxes
    draw_bbox:out_image            -> video_encoder:in_image
  }"""
```

> The `detect` flowunit directory must contain a `.toml` with `entry=./model.mlmodelc` and `device=apple_silicon`, `virtual_type=coreml`. See [references/coreml_inference.md](references/coreml_inference.md).

---

### Recipe 3: HTTP request → detect → JSON reply (chip-agnostic)

**References:** httpserver_async, image_process, inference/coreml_inference, yolo26_post

```toml
# graph/http_detect.toml
[graph]
  format = "graphviz"
  graphconf = """digraph http_detect {
    node [shape=Mrecord]

    http_in[type=flowunit, flowunit=httpserver_async, device=cpu,
            endpoint="/api/detect", port=8080]
    image_process[type=flowunit, flowunit=image_process, device=cuda,
                  width=640, height=640, color_mode="rgb", data_type="float"]
    detect[type=flowunit, flowunit=detect, device=cuda,
           virtual_type="tensorrt"]
    yolo26_post[type=flowunit, flowunit=yolo26_post, device=cpu,
                classes=80, conf_threshold=0.25, nms_threshold=0.45,
                input_width=640, input_height=640]

    http_in:out_request     -> image_process:in_image
    image_process:out_image -> detect:input
    detect:output           -> yolo26_post:in_data
    yolo26_post:out_data    -> http_in:in_reply
  }"""
```

> Swap `detect` `device`/`virtual_type` for Apple Silicon (device=apple_silicon, virtual_type=coreml) and replace `image_process` → `resize`.

---

### Recipe 4: RTSP → detect → output_broker (chip-agnostic)

**References:** video_demuxer, video_decoder, image_process, inference, yolo26_post, output_broker

```toml
# graph/rtsp_detect_broker.toml
[graph]
  format = "graphviz"
  graphconf = """digraph rtsp_detect_broker {
    node [shape=Mrecord]

    video_demuxer[type=flowunit, flowunit=video_demuxer, device=cpu]
    video_decoder[type=flowunit, flowunit=video_decoder, device=cuda,
                  pix_fmt="rgb"]
    image_process[type=flowunit, flowunit=image_process, device=cuda,
                  width=640, height=640, color_mode="rgb", data_type="float"]
    detect[type=flowunit, flowunit=detect, device=cuda,
           virtual_type="tensorrt"]
    yolo26_post[type=flowunit, flowunit=yolo26_post, device=cpu,
                classes=80, conf_threshold=0.25, nms_threshold=0.45,
                input_width=640, input_height=640]
    sink[type=flowunit, flowunit=output_broker, device=cpu,
         broker_type="http", broker_url="http://localhost:9090/results"]

    video_demuxer:out_video_packet -> video_decoder:in_video_packet
    video_decoder:out_image        -> image_process:in_image
    image_process:out_image        -> detect:input
    detect:output                  -> yolo26_post:in_data
    yolo26_post:out_data           -> sink:in_data
  }"""
```

> Wire the RTSP URL into `video_demuxer` via `source_url` or a preceding `data_source_generator` node. For Kafka output change `broker_type="kafka"` and set `broker_url` to the bootstrap server.
```

- [ ] **Step 2: Commit**

```bash
git add tools/modelbox-skill-recipes.md
git commit -m "feat(modelbox): add four pipeline recipe skeletons"
```

---

### Task 4: `tools/test-modelbox-skill.py` — Validator (write first, TDD)

**Files:**
- Create: `tools/test-modelbox-skill.py`

Write the validator before the generator so "passing the validator" is the definition of done for Task 5.

- [ ] **Step 1: Create `tools/test-modelbox-skill.py`**

```python
#!/usr/bin/env python3
"""test-modelbox-skill.py — Validate generated skills/modelbox/ artifacts.

Usage:
    python3 tools/test-modelbox-skill.py [skills/modelbox/]

Checks:
    1. SKILL.md has all seven required sections (non-empty bodies)
    2. SKILL.md frontmatter parses (name + description present)
    3. Catalog rows ↔ reference files are 1-to-1
    4. Every reference file has six required sections (non-empty bodies)
    5. Every TOML block in reference files parses cleanly
    6. Every generated file footer contains the pinned commit from the YAML
"""
import re
import sys
from pathlib import Path

try:
    import tomllib
except ImportError:
    try:
        import tomli as tomllib  # type: ignore[import]
    except ImportError:
        sys.exit("ERROR: Python 3.11+ required, or: pip install tomli")

try:
    import yaml
except ImportError:
    sys.exit("ERROR: pip install pyyaml")

REQUIRED_SKILL_SECTIONS = [
    "## When to use",
    "## Core mental model",
    "## Catalog",
    "## Virtual-type matrix",
    "## Known-good pipelines",
    "## Authoring gotchas",
    "## Pointers",
]

REQUIRED_REF_SECTIONS = [
    "## Purpose",
    "## TOML config",
    "## Ports",
    "## Config keys",
    "## Notes / gotchas",
    "## Upstream source",
]


def section_body(text: str, heading: str) -> str:
    """Return the text between `heading` and the next `## ` heading (or EOF)."""
    idx = text.find(heading)
    if idx == -1:
        return ""
    rest = text[idx + len(heading):]
    nxt = re.search(r"^## ", rest, re.MULTILINE)
    return rest[: nxt.start()] if nxt else rest


def check_sections(text: str, required: list[str], label: str) -> list[str]:
    errors: list[str] = []
    for sec in required:
        if sec not in text:
            errors.append(f"{label}: missing section '{sec}'")
        elif not section_body(text, sec).strip():
            errors.append(f"{label}: section '{sec}' is empty")
    return errors


def check_frontmatter(skill_text: str) -> list[str]:
    errors: list[str] = []
    if not skill_text.startswith("---"):
        errors.append("SKILL.md: does not start with YAML frontmatter '---'")
        return errors
    end = skill_text.find("---", 3)
    if end == -1:
        errors.append("SKILL.md: frontmatter has no closing '---'")
        return errors
    fm_str = skill_text[3:end].strip()
    try:
        fm = yaml.safe_load(fm_str)
        if not fm or not fm.get("name"):
            errors.append("SKILL.md: frontmatter missing 'name'")
        if not fm or not fm.get("description"):
            errors.append("SKILL.md: frontmatter missing 'description'")
    except yaml.YAMLError as exc:
        errors.append(f"SKILL.md: frontmatter YAML error: {exc}")
    return errors


def extract_catalog_refs(skill_text: str) -> set[str]:
    """Return set of flowunit names referenced in the Catalog section."""
    return set(re.findall(r"\(references/(\w+)\.md\)", skill_text))


def toml_blocks(text: str) -> list[str]:
    return re.findall(r"```toml\n(.*?)```", text, re.DOTALL)


def check_toml(block: str, label: str) -> list[str]:
    try:
        tomllib.loads(block)
        return []
    except Exception as exc:
        return [f"{label}: TOML parse error: {exc}"]


def check_pin(text: str, commit: str, label: str) -> list[str]:
    short = commit[:12]
    if commit not in text and short not in text:
        return [f"{label}: missing pin record (commit {short})"]
    return []


def load_allowlist_commit(tools_dir: Path) -> str:
    p = tools_dir / "modelbox-flowunits.yml"
    if not p.exists():
        return ""
    with open(p) as f:
        data = yaml.safe_load(f)
    return str(data.get("modelbox_commit", ""))


def main() -> None:
    skill_dir = Path(sys.argv[1]) if len(sys.argv) > 1 else Path("skills/modelbox")
    tools_dir = Path(__file__).parent

    errors: list[str] = []

    # ── Check skill_dir exists ──────────────────────────────────────────────
    if not skill_dir.is_dir():
        print(f"FAIL: {skill_dir} does not exist")
        sys.exit(1)

    skill_path = skill_dir / "SKILL.md"
    refs_dir = skill_dir / "references"

    if not skill_path.exists():
        print(f"FAIL: {skill_path} does not exist")
        sys.exit(1)

    skill_text = skill_path.read_text()

    # ── Load pinned commit ──────────────────────────────────────────────────
    expected_commit = load_allowlist_commit(tools_dir)

    # ── Check 1: SKILL.md seven sections ───────────────────────────────────
    errors += check_sections(skill_text, REQUIRED_SKILL_SECTIONS, "SKILL.md")

    # ── Check 2: Frontmatter ────────────────────────────────────────────────
    errors += check_frontmatter(skill_text)

    # ── Check 3: Catalog ↔ reference parity ────────────────────────────────
    catalog_refs = extract_catalog_refs(skill_text)
    existing_refs = {p.stem for p in refs_dir.glob("*.md")} if refs_dir.is_dir() else set()

    for name in sorted(catalog_refs - existing_refs):
        errors.append(f"Catalog references '{name}' but references/{name}.md not found")
    for name in sorted(existing_refs - catalog_refs):
        errors.append(f"references/{name}.md exists but is not referenced in Catalog")

    # ── Check 4 + 5 + 6: Per-reference-file checks ─────────────────────────
    if refs_dir.is_dir():
        for ref_path in sorted(refs_dir.glob("*.md")):
            ref_text = ref_path.read_text()
            label = f"references/{ref_path.name}"

            # Six sections present and non-empty
            errors += check_sections(ref_text, REQUIRED_REF_SECTIONS, label)

            # TOML validity
            for block in toml_blocks(ref_text):
                errors += check_toml(block, label)

            # Pin record
            if expected_commit:
                errors += check_pin(ref_text, expected_commit, label)

    # ── Check 6: SKILL.md pin record ───────────────────────────────────────
    if expected_commit:
        errors += check_pin(skill_text, expected_commit, "SKILL.md")

    # ── Report ──────────────────────────────────────────────────────────────
    if errors:
        print(f"FAIL: {len(errors)} error(s):")
        for e in errors:
            print(f"  - {e}")
        sys.exit(1)
    else:
        n_refs = len(existing_refs)
        print(f"OK: {n_refs} reference files, all checks passed (commit={expected_commit[:12]})")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Verify script is importable (no syntax errors)**

```bash
python3 -c "import ast, pathlib; ast.parse(pathlib.Path('tools/test-modelbox-skill.py').read_text()); print('syntax OK')"
```

Expected: `syntax OK`

- [ ] **Step 3: Run against empty dir — expect FAIL**

```bash
mkdir -p /tmp/modelbox-empty
python3 tools/test-modelbox-skill.py /tmp/modelbox-empty
```

Expected: `FAIL: /tmp/modelbox-empty/SKILL.md does not exist`

- [ ] **Step 4: Commit**

```bash
git add tools/test-modelbox-skill.py
git commit -m "feat(modelbox): add validator / six-section linter"
```

---

### Task 5: `tools/build-modelbox-skill.py` — Generator script

**Files:**
- Create: `tools/build-modelbox-skill.py`

Reads allowlist YAML + prelude + recipes, reads upstream example.toml where configured, generates SKILL.md and 19 reference .md files. Idempotent.

- [ ] **Step 1: Create `tools/build-modelbox-skill.py`**

```python
#!/usr/bin/env python3
"""build-modelbox-skill.py — Generate skills/modelbox/ from the upstream ModelBox repo.

Usage:
    python3 tools/build-modelbox-skill.py \
        --modelbox-repo /path/to/modelbox \
        --commit <full-sha> \
        --out skills/modelbox/

Requirements:
    pip install pyyaml tomli   # or Python 3.11+ (has tomllib)
"""
import argparse
import re
import sys
from pathlib import Path

try:
    import tomllib
except ImportError:
    try:
        import tomli as tomllib  # type: ignore[import]
    except ImportError:
        sys.exit("ERROR: Python 3.11+ required, or: pip install tomli")

try:
    import yaml
except ImportError:
    sys.exit("ERROR: pip install pyyaml")


# ── Templates ──────────────────────────────────────────────────────────────

SKILL_FRONTMATTER = '''\
---
name: modelbox
description: Use when authoring or modifying a ModelBox graph (.toml), configuring a ModelBox flowunit, choosing the right flowunit for a step (video decode, inference, YOLO postprocess, drawing boxes, encoding output), or resolving "which flowunit handles X". Provides a curated index of flowunits used in YOLO + video pipelines, with TOML config schema, ports, device options, and known-good recipes.
---

# ModelBox Flowunit Index

'''

CATALOG_HEADER = """\
## Catalog

| Flowunit | Devices | Group | Use for | Reference |
|----------|---------|-------|---------|-----------|
"""

REFERENCE_TEMPLATE = """\
---
generated: true
modelbox_commit: {commit}
modelbox_commit_date: {commit_date}
---

# `{name}`

**Devices:** {devices}
**Group:** {group}

## Purpose

{purpose}

## TOML config (`{name}.toml`)

```toml
{toml_content}
```

## Ports

{ports_md}

## Config keys

{config_keys_md}

## Notes / gotchas

{notes_md}

## Upstream source

- `{source_dir}/`

---
_Generated from modelbox@{commit} ({commit_date})._
"""

SKILL_FOOTER = """\
---
_Generated from modelbox@{commit} ({commit_date}). Re-run `make modelbox-skill` to update._
"""


# ── Helpers ─────────────────────────────────────────────────────────────────

def load_yaml(path: Path) -> dict:
    with open(path) as f:
        return yaml.safe_load(f)


def read_upstream_file(repo: Path, rel: str) -> str | None:
    if not rel:
        return None
    p = repo / rel
    return p.read_text() if p.exists() else None


def ports_to_md(ports: dict) -> str:
    lines: list[str] = []
    for inp in ports.get("inputs", []):
        lines.append(
            f"- **Input `{inp['name']}`:** {inp['type']} — {inp.get('notes', '')}"
        )
    for out in ports.get("outputs", []):
        lines.append(
            f"- **Output `{out['name']}`:** {out['type']} — {out.get('notes', '')}"
        )
    return "\n".join(lines) if lines else "- See upstream docs."


def config_keys_to_md(keys: list) -> str:
    if not keys:
        return "None required beyond `[base]`."
    rows = [
        "| Key | Required | Default | Notes |",
        "|-----|----------|---------|-------|",
    ]
    for k in keys:
        req = "yes" if k.get("required") else "no"
        default = str(k.get("default", "—"))
        notes = k.get("notes", "")
        rows.append(f"| `{k['key']}` | {req} | {default} | {notes} |")
    return "\n".join(rows)


def notes_to_md(notes: list) -> str:
    if not notes:
        return "- See upstream docs."
    return "\n".join(f"- {n}" for n in notes)


def extract_section(text: str, section_id: str) -> str:
    """Extract content after `<!-- SECTION: id -->` up to next marker or EOF."""
    pattern = rf"<!-- SECTION: {re.escape(section_id)} -->\n(.*?)(?=<!-- SECTION:|$)"
    m = re.search(pattern, text, re.DOTALL)
    if not m:
        raise ValueError(f"Section '{section_id}' not found in prelude/recipes file")
    return m.group(1).strip()


def build_catalog_row(fu: dict) -> str:
    name = fu["name"]
    devices = ", ".join(fu["devices"]) if isinstance(fu["devices"], list) else fu["devices"]
    group = fu["group"]
    use_for = fu["use_for"]
    return f"| `{name}` | {devices} | {group} | {use_for} | [references/{name}.md](references/{name}.md) |"


def make_toml_content(fu: dict, repo: Path) -> str:
    if fu.get("example_toml"):
        content = read_upstream_file(repo, fu["example_toml"])
        if content:
            return content.strip()
    fallback = fu.get("default_toml", "")
    if fallback:
        return fallback.strip()
    devices = fu["devices"]
    dev = devices[0] if isinstance(devices, list) else devices
    return (
        f'[base]\nname = "{fu["name"]}"\ndevice = "{dev}"\n'
        f'version = "1.0.0"\ntype = "{fu["name"]}"'
    )


def generate_reference(fu: dict, toml_content: str, commit: str, commit_date: str) -> str:
    devices = fu["devices"]
    devices_str = ", ".join(devices) if isinstance(devices, list) else devices
    return REFERENCE_TEMPLATE.format(
        commit=commit,
        commit_date=commit_date,
        name=fu["name"],
        devices=devices_str,
        group=fu["group"],
        purpose=fu.get("purpose", "").strip(),
        toml_content=toml_content,
        ports_md=ports_to_md(fu.get("ports", {})),
        config_keys_md=config_keys_to_md(fu.get("config_keys", [])),
        notes_md=notes_to_md(fu.get("notes", [])),
        source_dir=fu.get("source_dir", ""),
    )


# ── Main ─────────────────────────────────────────────────────────────────────

def main() -> None:
    parser = argparse.ArgumentParser(
        description="Generate skills/modelbox/ from upstream ModelBox repo."
    )
    parser.add_argument("--modelbox-repo", required=True, type=Path,
                        help="Path to cloned upstream modelbox repo")
    parser.add_argument("--commit", required=True,
                        help="Full SHA of the pinned commit")
    parser.add_argument("--out", required=True, type=Path,
                        help="Output directory (e.g. skills/modelbox/)")
    args = parser.parse_args()

    tools_dir = Path(__file__).parent
    allowlist_path = tools_dir / "modelbox-flowunits.yml"
    prelude_path = tools_dir / "modelbox-skill-prelude.md"
    recipes_path = tools_dir / "modelbox-skill-recipes.md"

    data = load_yaml(allowlist_path)
    commit = args.commit
    commit_date = str(data.get("modelbox_commit_date", ""))

    prelude_text = prelude_path.read_text()
    recipes_text = recipes_path.read_text()

    out = args.out
    refs_dir = out / "references"
    refs_dir.mkdir(parents=True, exist_ok=True)

    flowunits: list[dict] = data["flowunits"]

    # ── Generate reference files ────────────────────────────────────────────
    for fu in flowunits:
        toml_content = make_toml_content(fu, args.modelbox_repo)
        content = generate_reference(fu, toml_content, commit, commit_date)
        ref_path = refs_dir / f"{fu['name']}.md"
        ref_path.write_text(content)
        print(f"  wrote {ref_path}")

    # ── Assemble SKILL.md ───────────────────────────────────────────────────
    parts: list[str] = [SKILL_FRONTMATTER]

    parts.append(extract_section(prelude_text, "when_to_use") + "\n\n")
    parts.append(extract_section(prelude_text, "mental_model") + "\n\n")

    # Catalog (generated from YAML)
    parts.append(CATALOG_HEADER)
    for fu in flowunits:
        parts.append(build_catalog_row(fu) + "\n")
    parts.append("\n")

    parts.append(extract_section(prelude_text, "virtual_type_matrix") + "\n\n")
    parts.append(extract_section(recipes_text, "recipes") + "\n\n")
    parts.append(extract_section(prelude_text, "gotchas") + "\n\n")
    parts.append(extract_section(prelude_text, "pointers") + "\n\n")
    parts.append(SKILL_FOOTER.format(commit=commit, commit_date=commit_date))

    skill_path = out / "SKILL.md"
    skill_path.write_text("".join(parts))
    print(f"  wrote {skill_path}")
    print(f"\nDone. {len(flowunits)} flowunits, commit={commit[:12]}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: Verify syntax**

```bash
python3 -c "import ast, pathlib; ast.parse(pathlib.Path('tools/build-modelbox-skill.py').read_text()); print('syntax OK')"
```

Expected: `syntax OK`

- [ ] **Step 3: Commit**

```bash
git add tools/build-modelbox-skill.py
git commit -m "feat(modelbox): add generator script"
```

---

### Task 6: Generate skill files + validate + commit

**Files:**
- Create: `skills/modelbox/SKILL.md`
- Create: `skills/modelbox/references/*.md` (19 files)

- [ ] **Step 1: Install Python deps**

```bash
pip install pyyaml tomli 2>/dev/null || pip install pyyaml  # Python 3.11+ has tomllib built-in
```

- [ ] **Step 2: Clone upstream modelbox repo at pinned commit**

```bash
git clone --depth=200 https://github.com/modelbox-ai/modelbox.git /tmp/modelbox-upstream
git -C /tmp/modelbox-upstream checkout 125d1cd6b746cbfd410ac288a1e1f2e2664fb77e
```

Expected: Detached HEAD at `125d1cd6b746cbfd410ac288a1e1f2e2664fb77e`

- [ ] **Step 3: Run generator**

```bash
python3 tools/build-modelbox-skill.py \
    --modelbox-repo /tmp/modelbox-upstream \
    --commit 125d1cd6b746cbfd410ac288a1e1f2e2664fb77e \
    --out skills/modelbox/
```

Expected: 19 `wrote references/...md` lines + `wrote skills/modelbox/SKILL.md` + `Done. 19 flowunits, commit=125d1cd6b7`

- [ ] **Step 4: Run validator**

```bash
python3 tools/test-modelbox-skill.py skills/modelbox/
```

Expected: `OK: 19 reference files, all checks passed (commit=125d1cd6b746)`

If there are failures:
- "missing section" → check the prelude/recipes section markers match exactly what the validator looks for
- "TOML parse error" → the `default_toml` or upstream `example.toml` content is malformed; fix in allowlist YAML and re-run
- "missing pin record" → the REFERENCE_TEMPLATE or SKILL_FOOTER in the generator is missing `{commit}`; fix the template
- "not referenced in Catalog" → a ref file exists but the flowunit is missing from `tools/modelbox-flowunits.yml`

- [ ] **Step 5: Sanity check SKILL.md structure**

```bash
grep "^## " skills/modelbox/SKILL.md
```

Expected output (exactly seven headings):
```
## When to use
## Core mental model
## Catalog
## Virtual-type matrix
## Known-good pipelines
## Authoring gotchas
## Pointers
```

- [ ] **Step 6: Sanity check reference count**

```bash
ls skills/modelbox/references/ | wc -l
```

Expected: `19`

- [ ] **Step 7: Commit**

```bash
git add skills/modelbox/
git commit -m "feat(modelbox): add generated flowunit skill (19 refs, pin=125d1cd6)"
```

---

### Task 7: `.github/workflows/modelbox-bump.yml` — CI auto-bump

**Files:**
- Create: `.github/workflows/modelbox-bump.yml`

Runs daily at 04:00 UTC and on `workflow_dispatch`. Clones upstream, checks if HEAD matches the pinned SHA, runs generator + linter if not, opens/updates a PR with a diff summary. Fails hard on linter errors.

- [ ] **Step 1: Create `.github/workflows/modelbox-bump.yml`**

```yaml
name: modelbox-bump

on:
  schedule:
    - cron: "0 4 * * *"   # daily 04:00 UTC
  workflow_dispatch:

jobs:
  bump:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: pip install pyyaml

      - name: Read pinned commit from YAML
        id: pin
        run: |
          PINNED=$(python3 -c "
          import yaml
          d = yaml.safe_load(open('tools/modelbox-flowunits.yml'))
          print(d['modelbox_commit'])
          ")
          echo "sha=$PINNED" >> "$GITHUB_OUTPUT"

      - name: Clone upstream modelbox
        run: |
          git clone --depth=200 https://github.com/modelbox-ai/modelbox.git /tmp/modelbox-upstream

      - name: Get upstream HEAD SHA and date
        id: upstream
        run: |
          SHA=$(git -C /tmp/modelbox-upstream rev-parse HEAD)
          DATE=$(git -C /tmp/modelbox-upstream log -1 --format=%cs)
          echo "sha=$SHA" >> "$GITHUB_OUTPUT"
          echo "date=$DATE" >> "$GITHUB_OUTPUT"

      - name: Check if bump needed
        id: check
        run: |
          if [ "${{ steps.upstream.outputs.sha }}" = "${{ steps.pin.outputs.sha }}" ]; then
            echo "needed=false" >> "$GITHUB_OUTPUT"
            echo "Pinned commit is already up to date — no-op."
          else
            echo "needed=true" >> "$GITHUB_OUTPUT"
            echo "New upstream SHA: ${{ steps.upstream.outputs.sha }}"
          fi

      - name: Update pinned commit in YAML
        if: steps.check.outputs.needed == 'true'
        run: |
          python3 - <<'PYEOF'
          import yaml
          from pathlib import Path

          p = Path("tools/modelbox-flowunits.yml")
          d = yaml.safe_load(p.read_text())
          d["modelbox_commit"] = "${{ steps.upstream.outputs.sha }}"
          d["modelbox_commit_date"] = "${{ steps.upstream.outputs.date }}"
          p.write_text(yaml.dump(d, default_flow_style=False, allow_unicode=True, sort_keys=False))
          PYEOF

      - name: Run generator
        if: steps.check.outputs.needed == 'true'
        run: |
          python3 tools/build-modelbox-skill.py \
            --modelbox-repo /tmp/modelbox-upstream \
            --commit "${{ steps.upstream.outputs.sha }}" \
            --out skills/modelbox/

      - name: Run linter / validator
        if: steps.check.outputs.needed == 'true'
        run: python3 tools/test-modelbox-skill.py skills/modelbox/

      - name: Compute diff summary
        if: steps.check.outputs.needed == 'true'
        id: diff
        run: |
          SUMMARY=$(git diff --stat skills/modelbox/ tools/modelbox-flowunits.yml 2>/dev/null || echo "(no diff)")
          echo "summary<<__EOF__" >> "$GITHUB_OUTPUT"
          echo "$SUMMARY" >> "$GITHUB_OUTPUT"
          echo "__EOF__" >> "$GITHUB_OUTPUT"

      - name: Create or update PR
        if: steps.check.outputs.needed == 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          NEW_SHA: ${{ steps.upstream.outputs.sha }}
          NEW_DATE: ${{ steps.upstream.outputs.date }}
          DIFF_SUMMARY: ${{ steps.diff.outputs.summary }}
        run: |
          SHORT="${NEW_SHA:0:8}"
          BRANCH="automation/modelbox-bump-${SHORT}"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git checkout -b "$BRANCH" 2>/dev/null || git checkout "$BRANCH"
          git add skills/modelbox/ tools/modelbox-flowunits.yml
          git commit -m "chore(modelbox): bump pin to ${SHORT} (${NEW_DATE})"
          git push origin "$BRANCH" --force

          EXISTING=$(gh pr list --head "$BRANCH" --json number --jq '.[0].number // empty')
          PR_BODY="## modelbox bump to \`${SHORT}\` (${NEW_DATE})

          ### Changed files

          \`\`\`
          ${DIFF_SUMMARY}
          \`\`\`

          ### Notes

          - Auto-generated by \`modelbox-bump\` workflow.
          - If a flowunit disappeared upstream (MISSING marker in linter), this PR will not exist — the job fails instead. Manually update \`tools/modelbox-flowunits.yml\`.
          - Review and merge; do NOT auto-merge (human review required).
          "

          if [ -n "$EXISTING" ]; then
            gh pr edit "$EXISTING" --body "$PR_BODY"
            echo "Updated existing PR #${EXISTING}"
          else
            gh pr create \
              --title "chore(modelbox): bump pin to ${SHORT}" \
              --body "$PR_BODY" \
              --base main \
              --head "$BRANCH" \
              --label "automation"
            echo "Opened new PR for branch ${BRANCH}"
          fi
```

- [ ] **Step 2: Verify YAML is syntactically valid**

```bash
python3 -c "import yaml; yaml.safe_load(open('.github/workflows/modelbox-bump.yml')); print('YAML OK')"
```

Expected: `YAML OK`

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/modelbox-bump.yml
git commit -m "feat(modelbox): add daily CI auto-bump workflow"
```

---

### Task 8: Add Makefile target + `tools/README.md` + version bump + validate + push

**Files:**
- Create: `tools/README.md` (maintainer docs)
- Modify: `.claude-plugin/plugin.json` (version 0.3.0 → 0.4.0)
- Modify: `.claude-plugin/marketplace.json` (version 0.3.0 → 0.4.0)
- Modify: `README.md` (mention modelbox skill)

- [ ] **Step 1: Create `tools/README.md`**

```markdown
# tools/

Maintainer tooling for the `modelbox` skill.

## Files

| File | Purpose |
|------|---------|
| `modelbox-flowunits.yml` | Allowlist: 19 flowunits, pinned upstream SHA, all metadata |
| `modelbox-skill-prelude.md` | Hand-written SKILL.md sections (non-catalog) |
| `modelbox-skill-recipes.md` | Four pipeline recipe skeletons |
| `build-modelbox-skill.py` | Generator: reads YAML + prelude + recipes → `skills/modelbox/` |
| `test-modelbox-skill.py` | Validator: six-section linter, parity, TOML validity, pin record |

## Regenerate skill files

```bash
# 1. Clone upstream at pinned commit (or update to a new one)
COMMIT=$(python3 -c "import yaml; print(yaml.safe_load(open('tools/modelbox-flowunits.yml'))['modelbox_commit'])")
git clone --depth=200 https://github.com/modelbox-ai/modelbox.git /tmp/modelbox-upstream
git -C /tmp/modelbox-upstream checkout "$COMMIT"

# 2. Generate
python3 tools/build-modelbox-skill.py \
    --modelbox-repo /tmp/modelbox-upstream \
    --commit "$COMMIT" \
    --out skills/modelbox/

# 3. Validate
python3 tools/test-modelbox-skill.py skills/modelbox/
```

## Bump the pinned commit manually

Edit `modelbox_commit` and `modelbox_commit_date` in `tools/modelbox-flowunits.yml`, then re-run steps 2 and 3 above.

## Add a new flowunit

1. Add an entry to `tools/modelbox-flowunits.yml` following the existing schema.
2. Regenerate and validate.
3. The new flowunit appears in the catalog automatically.

## CI auto-bump

`.github/workflows/modelbox-bump.yml` runs daily and opens a PR when upstream HEAD diverges from the pin. The PR includes a diff summary. Maintainers review and merge; the workflow never auto-merges.
```

- [ ] **Step 2: Bump version in `.claude-plugin/plugin.json`**

Change `"version": "0.3.0"` to `"version": "0.4.0"`:

```json
{
  "name": "ultralytics",
  "version": "0.4.0",
  "description": "Train, predict, export, and validate datasets with Ultralytics YOLO from inside Claude Code.",
  "author": {
    "name": "Ultralytics",
    "url": "https://ultralytics.com"
  },
  "homepage": "https://github.com/ultralytics/ultralytics",
  "keywords": ["yolo", "ultralytics", "computer-vision", "object-detection"]
}
```

- [ ] **Step 3: Bump version in `.claude-plugin/marketplace.json`**

Change `"version": "0.3.0"` to `"version": "0.4.0"`:

```json
{
  "name": "ultralytics-plugins",
  "owner": {
    "name": "Ultralytics",
    "email": "hello@ultralytics.com"
  },
  "description": "Claude Code plugins for Ultralytics YOLO workflows.",
  "plugins": [
    {
      "name": "ultralytics",
      "source": "./",
      "description": "Train, predict, export, and validate datasets with Ultralytics YOLO.",
      "version": "0.4.0",
      "homepage": "https://github.com/Bovey0809/claude-code-ultralytics",
      "repository": "https://github.com/Bovey0809/claude-code-ultralytics",
      "category": "computer-vision",
      "tags": ["yolo", "ultralytics", "object-detection", "computer-vision"]
    }
  ]
}
```

- [ ] **Step 4: Add modelbox skill mention to `README.md`**

Add after the existing `## What you get` bullet list (after the `dataset-check` bullet):

```markdown
- **Skill `/ultralytics:modelbox`** — curated index of 19 ModelBox flowunits (TOML config schema, ports, device options, known-good pipeline recipes for NVIDIA + Apple Silicon). Claude consults this when authoring ModelBox deployment graphs.
```

- [ ] **Step 5: Validate plugin**

```bash
claude plugin validate .
```

Expected: `Plugin validation passed` (or equivalent success output).

- [ ] **Step 6: Commit and push**

```bash
git add tools/README.md .claude-plugin/plugin.json .claude-plugin/marketplace.json README.md
git commit -m "chore: bump plugin to v0.4.0; add modelbox skill to README"
git push
```

---

## Self-review

**Spec coverage check:**

| Spec requirement | Task |
|-----------------|------|
| `tools/modelbox-flowunits.yml` allowlist (19 flowunits, pinned SHA) | Task 1 |
| `tools/modelbox-skill-prelude.md` (when-to-use, mental model, matrix, gotchas, pointers) | Task 2 |
| `tools/modelbox-skill-recipes.md` (4 recipes: NV file, Apple file, HTTP, RTSP) | Task 3 |
| `tools/test-modelbox-skill.py` (5 checks: sections, frontmatter, parity, TOML validity, pin) | Task 4 |
| `tools/build-modelbox-skill.py` generator (idempotent, footer pin, six-section refs) | Task 5 |
| `skills/modelbox/SKILL.md` + 19 `references/*.md` (generated, checked in) | Task 6 |
| `.github/workflows/modelbox-bump.yml` (daily cron, opens PR, fails on missing flowunit) | Task 7 |
| Version bump to 0.4.0, plugin validate | Task 8 |

**Placeholder scan:** No TBD, TODO, or "similar to Task N" patterns present.

**Type consistency:** Generator function names are consistent across tasks (extract_section, ports_to_md, config_keys_to_md, notes_to_md). Validator section list `REQUIRED_SKILL_SECTIONS` matches the seven sections the generator emits. Section markers in prelude (`<!-- SECTION: when_to_use -->` etc.) match `extract_section()` calls in the generator.
